{% extends 'base.html.twig' %}

{% block title %}Espace client | JVGO{% endblock %}

{% block stylesheets %}
    <style>
        #navigation {
            background: #000!important;
        }
        .section-padding {
            padding: 150px 0px!important;
        }
        .tabs-left, .tabs-right {
            border-bottom: none;
            padding-top: 2px;
        }
        .tabs-left {
            border-radius:2px;
            box-shadow: rgb(100 100 111 / 20%) 0px 7px 29px 0px;
            display: block;
        }
        .tab-content {
            box-shadow: rgb(100 100 111 / 20%) 0px 7px 29px 0px;
            border-radius: 2px;
            min-height: 100%;
        }
        .tabs-left>li, .tabs-right>li {
            float: none;
        }
        .tabs-left>li {
            margin-right: -1px;
            border-top: 1px solid #ddd;
        }
        .tab-pane-head {
            padding: 10px 15px;
            background: #434859;
            color: white;
        }
        .tab-pane-body {
            padding: 10px 15px;
        }
        .tabs-left>li>a {
            position: relative;
            display: block;
            padding: 10px 15px;
        }
        .tabs-left>li.active {
            background: #434859;
        }
        .tabs-left>li.active>a {
            color: white;
        }
        .progressbar {
            margin: 50px 0 50px 0;
            counter-reset: step;
        }
        .progressbar li {
            list-style-type: none;
            width: 20%;
            float: left;
            font-size: 12px;
            position: relative;
            text-align: center;
            text-transform: uppercase;
            color: #000;
        }
        .progressbar li p {
            font-size: 11px;
            text-transform: none;
            line-height: 1rem;
        }
        .progressbar li:before {
            width: 15px;
            height: 15px;
            content: '';
            line-height: 30px;
            border: 2px solid #cccfd8;
            background-color: #cccfd8;
            display: block;
            text-align: center;
            margin: 0 auto 10px auto;
            border-radius: 50%;
            transition: all .8s;
        }
        .progressbar li:after {
            width: 100%;
            height: 2px;
            content: '';
            position: absolute;
            background-color: #cccfd8;
            top: 7px;
            left: -50%;
            z-index: -1;
            transition: all .8s;
        }
        .progressbar li:first-child:after {
            content: none;
        }
        .progressbar li.active:before {
            border-color: #434859;
            background-color: #434859;
            transition: all .8s;
        }
        .progressbar li.active:after {
            background-color: #434859;
            transition: all .8s;
        }

    </style>
{% endblock %}

{% block body %}

<!-- START HOME -->
<section class="section-padding">
    <div class="container">
        <div class="row" style="min-height:200px;">
            <div class="col-md-3">
                <!-- required for floating -->
                <!-- Nav tabs -->
                <ul class="nav nav-tabs tabs-left">
                    <li style="border: none; padding: 10px;">
                        <div style="display: flex;">
                            <img src="/assets/img/avatar_user.jpeg" style="width: 56px; height: 56px; border-radius: 50%">
                            <p style="margin-bottom: auto; margin-top: auto;">{{app.user.nom}}</p>
                        </div>
                    </li>
                    <li class="active"><a href="{{path('user')}}"><i class="fa fa-folder"></i> Mon dossier</a>

                    </li>
                    <li><a href="{{path('user.profil')}}"><i class="fa fa-key"></i> Mes accès</a>

                    </li>
                    <li><a href="{{path('home')}}#contact"><i class="fa fa-envelope"></i> Contactez-nous</a>

                    </li>
                    <li><a href="{{path('logout')}}"><i class="fa fa-power-off"></i> Se deconnecter</a>

                    </li>
                </ul>
            </div>
            <div class="col-md-9">
                <!-- Tab panes -->
                <div class="tab-content">
                    <div class="tab-pane active" id="dossier">
                        <div class="tab-pane-head">
                            Mon dossier
                        </div>
                        <div class="tab-pane-body">
                            <p>Le statut de votre dossier est mise à jour par l'administrateur dès qu'il passe à une étape suivante. Pour plus d'information ou réclamation, vous pouvez <a href="{{path('home')}}#contact" style="color: #434859; text-decoration: underline;">nous contacter</a>.</p>

                            <p>Le numéro de votre dossier : <strong>{{app.user.numero}}</strong></p>
                            <ul id="progress-bar" class="progressbar">
                                <li class="{{statut >= 1 ? 'active' : ''}}"><p>Demande envoyer</p></li>
                                <li class="{{statut >= 2 ? 'active' : ''}}"><p>Demande en cours de traitement</p></li>
                                <li class="{{statut >= 3 ? 'active' : ''}}"><p>Terminer</p></li>
                            </ul>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="clearfix"></div>
        </div>
    </div>
</section>
<!-- END  HOME DESIGN -->
{% endblock %}
